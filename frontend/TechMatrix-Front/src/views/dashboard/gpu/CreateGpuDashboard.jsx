export default function CreateGpuDashboard() {
    return (
        <>
            <main>
                <button
                    className="backBtn"
                    onClick={() => window.history.back()}
                >
                    Back
                </button>
                <h1>Create new gpu</h1>
            </main>
        </>
    );
}
